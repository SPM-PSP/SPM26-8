/**
 * 使用说明：
 * 1. 复制本文件为同目录下的 ai.config.ts
 * 2. 在 apiKey 引号内粘贴你的 DeepSeek Key
 * 3. 保存后重启 npm run dev
 *
 * ai.config.ts 已加入 .gitignore，避免 Key 被提交到 Git
 */
export const AI_CONFIG = {
  apiKey: 'sk-在这里粘贴你的DeepSeek密钥',
  baseUrl: 'https://api.deepseek.com',
  model: 'deepseek-chat',
};
